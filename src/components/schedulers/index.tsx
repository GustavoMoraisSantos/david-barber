import SchedulerCalendar from "./Calendar";
import "./calendar.css";

export default function Scheduler() {
  return (
    <div className={"mainContainer"}>
      <SchedulerCalendar />
    </div>
  );
}
